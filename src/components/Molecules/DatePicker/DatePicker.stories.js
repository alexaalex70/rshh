import { storiesOf } from '@storybook/react-native';
import React from 'react';
import DatePicker from './DatePicker';

storiesOf('— Molecules', module).add('Single date picker', () => (
  <DatePicker />
));
