import { storiesOf } from '@storybook/react-native';
import React from 'react';
import AppBarProgress from './AppBarProgress';

storiesOf('— Molecules', module).add('App Bar with progress', () => (
  <AppBarProgress />
));
